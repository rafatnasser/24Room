name: Munasabati v5.3.0 Calendar Sources

on:
  push:
    branches: [ "munasabati-v5-build" ]
    paths:
      - '.github/workflows/munasabati-v5.3.0-calendar-sources.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  build-test-upgrade:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Restore exact v5.2.6 source
        shell: bash
        run: |
          set -euo pipefail
          cat .munasabati-v5-v502-brand/source.part*.b64 > /tmp/v502.b64
          base64 --decode /tmp/v502.b64 > /tmp/v502.tgz
          mkdir -p v5src
          tar -xzf /tmp/v502.tgz -C v5src
          python .munasabati-v5.0.3-final/apply_userfix503.py v5src
          base64 --decode .munasabati-v5.0.4/v504.patch.gz.b64 | gunzip > /tmp/v504.patch
          patch -p1 -d v5src < /tmp/v504.patch
          cat .munasabati-v5.1.0/parts/part* > /tmp/v510.patch.gz.b64
          base64 --decode /tmp/v510.patch.gz.b64 | gunzip > /tmp/v510.patch
          patch -p1 -d v5src < /tmp/v510.patch
          python .munasabati-v5.2.0/apply_v520.py v5src .munasabati-v5.2.0
          python .munasabati-v5.2.1/apply_v521.py v5src
          python .munasabati-v5.2.2/apply_v522.py v5src .munasabati-v5.2.2
          python .munasabati-v5.2.3/apply_v523.py v5src .munasabati-v5.2.3
          python .munasabati-v5.2.4/apply_v524.py v5src .munasabati-v5.2.4
          python .munasabati-v5.2.5/apply_v525.py v5src .munasabati-v5.2.5
          python .munasabati-v5.2.6/apply_v526.py v5src .munasabati-v5.2.6
          grep -q 'versionCode = 86' v5src/app/build.gradle.kts
          grep -q 'versionName = "5.2.6"' v5src/app/build.gradle.kts

      - uses: actions/setup-java@v5
        with: { distribution: temurin, java-version: '17' }
      - uses: android-actions/setup-android@v3
      - run: sdkmanager "platforms;android-36" "build-tools;36.0.0" "platform-tools"
      - uses: gradle/actions/setup-gradle@v4
        with: { gradle-version: '9.3.1' }

      - name: Build v5.2.6 baseline
        shell: bash
        run: |
          set -euo pipefail
          gradle -p v5src :app:assembleStableRelease --stacktrace --no-daemon
          cp "$(find v5src/app/build/outputs/apk/stable/release -type f -name '*.apk' | head -1)" /tmp/v526-unsigned.apk

      - name: Apply v5.3.0 fixes
        shell: bash
        run: |
          set -euo pipefail
          python .munasabati-v5.3.0/apply_v530.py v5src .munasabati-v5.3.0
          grep -q 'versionCode = 90' v5src/app/build.gradle.kts
          grep -q 'versionName = "5.3.0"' v5src/app/build.gradle.kts
          grep -q 'CalendarContract.Instances.CONTENT_URI' v5src/app/src/main/java/com/rafat/munasabati/compat/CalendarSync.kt
          grep -q 'CUSTOM_APP_PACKAGE' v5src/app/src/main/java/com/rafat/munasabati/compat/CalendarSync.kt
          grep -q 'AhlBaytCalendarEventBridge.eventsForDate' v5src/app/src/main/java/com/rafat/munasabati/ui/V52Experience.kt
          grep -q 'AhlBaytCalendarEventBridge.eventsForMonth' v5src/app/src/main/java/com/rafat/munasabati/ui/V52Experience.kt
          ! grep -q 'cleanupLegacyCalendarMatches' v5src/app/src/main/java/com/rafat/munasabati/compat/CalendarSync.kt
          test -f v5src/app/src/androidTest/java/com/rafat/munasabati/compat/V530CalendarIntegrationTest.kt

      - name: Unit tests and v5.3.0 builds
        shell: bash
        run: |
          set -euo pipefail
          gradle -p v5src :app:testStableDebugUnitTest --stacktrace --no-daemon
          gradle -p v5src :app:assembleStableDebug :app:assembleStableRelease :app:assembleStableDebugAndroidTest --stacktrace --no-daemon
          cp "$(find v5src/app/build/outputs/apk/stable/release -type f -name '*.apk' | head -1)" /tmp/v530-unsigned.apk

      - name: Sign upgrade pair with one test certificate
        shell: bash
        run: |
          set -euo pipefail
          keytool -genkeypair -keystore /tmp/test.jks -storepass testpass -keypass testpass -alias test -keyalg RSA -keysize 2048 -validity 3650 -dname "CN=Munasabati 530 Test,O=Munasabati,C=SA"
          for v in 526 530; do
            "$ANDROID_HOME/build-tools/36.0.0/zipalign" -f -p 4 "/tmp/v${v}-unsigned.apk" "/tmp/v${v}-aligned.apk"
            java -jar "$ANDROID_HOME/build-tools/36.0.0/lib/apksigner.jar" sign --ks /tmp/test.jks --ks-key-alias test --ks-pass pass:testpass --key-pass pass:testpass --out "/tmp/v${v}.apk" "/tmp/v${v}-aligned.apk"
            java -jar "$ANDROID_HOME/build-tools/36.0.0/lib/apksigner.jar" verify --verbose "/tmp/v${v}.apk"
          done

      - name: Enable KVM
        shell: bash
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Android 15 calendar integration and data-preserving upgrade
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: pixel_7
          disable-animations: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          script: |
            set -e
            gradle -p v5src :app:connectedStableDebugAndroidTest --stacktrace --no-daemon
            adb uninstall com.rafat.munasabati || true
            adb install /tmp/v526.apk
            adb root || true
            adb wait-for-device
            adb shell pm grant com.rafat.munasabati android.permission.POST_NOTIFICATIONS || true
            adb shell am start -W -n com.rafat.munasabati/com.rafat.munasabati.MainActivity
            sleep 8
            adb shell mkdir -p /data/user/0/com.rafat.munasabati/files
            adb shell 'echo keep-v530 > /data/user/0/com.rafat.munasabati/files/v530-upgrade-sentinel.txt'
            adb logcat -c || true
            adb install -r /tmp/v530.apk
            adb shell pm grant com.rafat.munasabati android.permission.POST_NOTIFICATIONS || true
            adb shell am force-stop com.rafat.munasabati
            adb shell am start -W -n com.rafat.munasabati/com.rafat.munasabati.MainActivity
            sleep 12
            adb shell pidof com.rafat.munasabati
            adb shell cat /data/user/0/com.rafat.munasabati/files/v530-upgrade-sentinel.txt | grep -q keep-v530
            adb shell dumpsys package com.rafat.munasabati | grep -q 'versionCode=90'
            adb logcat -d > /tmp/v530.log 2>/tmp/v530-logcat.err || true
            test -z "$(grep -E 'FATAL EXCEPTION|Process: com.rafat.munasabati' /tmp/v530.log || true)"
            echo V530_CALENDAR_SOURCES_AND_UPGRADE_PASS

      - name: Package validated release
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p final
          cp /tmp/v530-unsigned.apk final/Munasabati-v5.3.0-FINAL-unsigned.apk
          cp "$ANDROID_HOME/build-tools/36.0.0/lib/apksigner.jar" final/apksigner.jar
          cp "$ANDROID_HOME/build-tools/36.0.0/zipalign" final/zipalign
          sha256sum final/Munasabati-v5.3.0-FINAL-unsigned.apk > final/unsigned-sha256.txt
          printf 'version=5.3.0\nversionCode=90\nlocal-history-never-heuristically-deleted=PASS\ncalendar-instances-recurring-sync=PASS\nexternal-expiry-explicit-identity-only=PASS\nahl-bayt-occasions-in-calendar=PASS\nlocal-calendar-marker=PASS\nunit-tests=PASS\nandroid-tests=PASS\nupgrade-5.2.6-to-5.3.0=PASS\n' > final/build-info.txt

      - uses: actions/upload-artifact@v4
        with:
          name: Munasabati-v5.3.0-final-upgrade-passed
          path: final/*
          if-no-files-found: error
          retention-days: 7
